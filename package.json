{
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"cd backend && source venv/bin/activate && uvicorn main:app --reload --port 8000\""
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
